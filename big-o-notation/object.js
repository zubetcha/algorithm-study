const zubetcha = {
  name: 'juhye',
  age: 29,
  liveInSeoul: true,
};

// 객체에 접근, 추가, 제거하는 작업은 O(1) -> 빠르다.
// 탐색은 O(n)
// Object.keys(), Object.values(), Object.entries -> key의 개수에 따라 실행 속도가 달라짐 -> O(n)
// Object.hasOwnProperty()는 객체의 접근과 비슷 -> O(1)
